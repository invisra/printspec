# Units and coordinates

printspec 0.2.0 supports millimeters (`mm`) only. X is length, Y is width/depth, and Z is height/thickness. The origin is generally centered in X/Y, and generated CAD generally places the bottom face at Z = 0. Component positions use center coordinates unless a relation says otherwise.
