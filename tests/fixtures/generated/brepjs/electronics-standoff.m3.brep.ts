// Generated by printspec 0.2.0
// Review generated CAD before manufacturing.
import { box, cylinder, shape } from 'brepjs';

const outerDiameter = 7;
const height = 10;
const holeDiameter = 3.2;
const baseDiameter = 10;
const baseHeight = 2;

let part = shape(cylinder(baseDiameter / 2, baseHeight))
  .fuse(shape(cylinder(outerDiameter / 2, height)).translate([0, 0, baseHeight]).val).val;
part = shape(part)
  .cut(shape(cylinder(holeDiameter / 2, baseHeight + height + 0.2)).translate([0, 0, -0.1]).val).val;

export default () => part;
