import type {GeneratorResult,PrintSpec} from '../types.js';
import {validatePrintSpec} from '../validate.js';
function invalid(spec:PrintSpec){const r=validatePrintSpec(spec); return r.valid?null:{supported:false,code:'',message:`Validation failed: ${r.errors.slice(0,3).join('; ')}`};}
const holes=(hs:any[]=[])=>hs.map(h=>`  translate([${h.x}, ${h.y}, -0.1]) cylinder(h = height + 0.2, d = ${h.diameter}, $fn = 48);`).join('\n');
export function generateOpenScad(spec:PrintSpec):GeneratorResult{const bad=invalid(spec); if(bad) return bad; const p:any=spec.part; if(!p) return {supported:false,code:'',message:'Only part specs are supported.'}; const a=p.parameters; const header='// Generated by printspec 0.1.0\n// Review generated CAD before manufacturing.\n'; if(p.type==='rounded_rectangular_plate'){return {supported:true,code:`${header}length = ${a.length};\nwidth = ${a.width};\nheight = ${a.thickness};\ncorner_radius = ${a.cornerRadius};\n\nmodule rounded_plate() {\n  hull() {\n    for (x = [-length/2 + corner_radius, length/2 - corner_radius])\n      for (y = [-width/2 + corner_radius, width/2 - corner_radius])\n        translate([x, y, 0]) cylinder(h = height, r = corner_radius, $fn = 32);\n  }\n}\n\ndifference() {\n  rounded_plate();\n${holes(a.holes)}\n}\n`};} if(p.type==='spacer_block'){return {supported:true,code:`${header}length = ${a.length};\nwidth = ${a.width};\nheight = ${a.height};\n\ndifference() {\n  translate([-length/2, -width/2, 0]) cube([length, width, height]);\n${holes(a.holes)}\n}\n`};} return {supported:false,code:'',message:`Unsupported part family: ${p.type}`};}
